columns:
    id:
        label: id
        type: number
    created_at:
        label: created_at
        type: datetime
    updated_at:
        label: updated_at
        type: datetime
    order_id:
        label: order_id
        type: number
    # old_data:
    #     label: old_data
    #     type: text
    # new_data:
    #     label: new_data
    #     type: text
    difference:
        label: difference
        type: number
    status:
        label: status
        type: switch
